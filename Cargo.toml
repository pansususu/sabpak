[package]
name = "sabpak"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
ureq = { version = "2", features = ["json"] }

[profile.release]
lto = true
strip = true
codegen-units = 1
panic = "abort"
